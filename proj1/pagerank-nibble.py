import networkx as nx

def pagerank_nibble(graph, initial_vertex, connectivity, volume):
    """
    """
    alpha = connectivity
    pagerank_vector = aproximate_pagerank(graph)

def aproximate_pagerank(graph, starting_vector, prob_teleport, aprox_ratio):
